{
  "name": "@console-capture/extension",
  "version": "1.0.0",
  "description": "Chrome Extension for ConsoleCapture",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts,.tsx",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "package": "npm run build && cd dist && zip -r ../console-capture-extension.zip ."
  },
  "dependencies": {
    "@console-capture/shared": "workspace:*",
    "webextension-polyfill": "^0.10.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.258",
    "@types/jest": "^29.5.10",
    "@types/webextension-polyfill": "^0.10.0",
    "copy-webpack-plugin": "^11.0.0",
    "css-loader": "^6.8.1",
    "html-webpack-plugin": "^5.5.4",
    "jest": "^29.7.0",
    "style-loader": "^3.3.3",
    "ts-jest": "^29.1.1",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
